name: my-workflow

on:
  push:
    branches:
      - main

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: second-step
        run: |
          echo "Hello World"
          echo "This is the second step new file"

       